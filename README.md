# Tic-Tac-Toe «Крестики-нолики»

Проект сделан по [туториалу React](https://react.dev/learn/tutorial-tic-tac-toe).

Цель: - повторить/систематизировать основы React

В процессе стало интересно автоматизировать создание компонентов. А так же решила добавить смену темы. 

---

## 🛠️ Стек технологий

* **React**
* **TypeScript**
* **SCSS Modules**
* **clsx** — для условных классов
* **generate-react-cli** — для автоматизации создания компонентов
* **Vite**
* **FSD** — основная архитектура
* **Atomic Design** — архитектура для UI компонентов
* **BEM** — нейминг для компонентов

##
* **SCSS variables/mixins/tokens** — для темизации и автоматического формирования модификаторов

Пример полиморфного реакт-компонента Typography.tsx

Отличная идея 👍
Такой README — **очень сильный плюс** и для саморазвития, и для ревью, и для портфолио.

Ниже — **готовый README**, который ты можешь положить в корень проекта и при желании адаптировать под себя.

---

# Tic-Tac-Toe (React + Atomic Design)

Учебный проект «Крестики-нолики», реализованный на React с упором на:

* архитектуру компонентов
* Atomic Design
* читаемость и поддерживаемость кода
* осознанные структурные решения

Проект развивается без готового UI-макета, с постепенным формированием дизайн-системы.

---

## 🛠️ Стек технологий

* **React**
* **TypeScript**
* **SCSS Modules**
* **clsx** — для условных классов
* **Vite** (или CRA — при необходимости указать)
* **Atomic Design** — архитектура компонентов

---

## 📁 Структура проекта лайт версия смеси FSD + Atomic Design

```
src/
    app/              # Слой главного приложения
        styles/       # Глобальные переменные и сброс стилей
    features/         # Слой компонентов с бизнесс-логикой (Game)
    shared/           # Слой shared
        styles/       # Общие переменные и токены 
        types/        # Общие TypeScript-типы
        utils/        # Утилитарные функции (calculateWinner)

    ui/               # Слой UI (общий)
        atoms/        # Минимальные UI-компоненты (Button, Typography)
        molecules/    # Комбинации атомов (HistoryStepItem)
        organisms/    # Составные UI-блоки (Stepper, Board, StatusPanel)
        layouts/      # Компоновка страниц (GameLayout)

```

---

## 🎮 Логика игры

* История ходов хранится в массиве `history`
* Каждый ход содержит:

    * номер шага (`step`)
    * состояние поля
    * координаты хода
    * текущего игрока
* Навигация по истории возможна в прямом и обратном порядке
* Сортировка истории влияет **только на отображение**, но не на состояние игры

---

## 🎨 Стили и нейминг

* **SCSS Modules**
* **camelCase** — для JS/TS
* **PascalCase** — для компонентов (каждый компонент имеет className даже если не содержит стилей, для удобной идентификации компонентов в DevTools)
* **BEM-like** нейминг внутри SCSS-модулей (BlockName_ElemName_modName_modVal)
* Используется `clsx` для условных классов

---

## 🚀 Возможные улучшения

* Добавить unit-тесты
* Добавить интернационализацию
* Перевести логику игры на `useReducer`
* Добавить accessibility (ARIA)
* Добавить игру с компьютером
* Добавить возможность выбора цвета для символа игрока
* Улучшить адаптивность 

---

## 📌 Примечание

Проект развивается итеративно.
Решения могут пересматриваться по мере роста сложности и требований.
