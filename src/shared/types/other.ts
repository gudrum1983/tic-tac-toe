import { PLAYERS } from '@utils';

export type HistoryItem = {
  step: number,
  value: Array<string | null>,
  currentPlayer?: string,
  coordinates?: [number, number]
};

export type Player = typeof PLAYERS[keyof typeof PLAYERS];

export type ThemeName = 'theme-light' | 'theme-dark';
export type ThemeKey = 'LIGHT' | 'DARK';

export type Themes =  Array<{name: ThemeName, label: string}>;


/*todo: https://habr.com/ru/articles/649125/ - прочитать про enum
export enum ThemesToChangeEnum {
  LIGHT = 'light',
  DARK = 'dark',
};

export type ThemesToChange = ThemesToChangeEnum.LIGHT | ThemesToChangeEnum.DARK;

🇷🇺 На русском языке (Habr и переводы)
"TypeScript enums: вред или польза?" (Habr) В таких статьях обычно разбирают, во что компилируются энамы, и показывают проблемы числовых энамов (reverse mapping).
Ключевая мысль: enum добавляет вес в бандл и ведет себя не всегда предсказуемо.
Поисковый запрос: habr typescript enum зло или habr typescript enum as const.
"Перестаньте использовать enum в TypeScript" (YouTube/Статьи) Многие русскоязычные блогеры делают разборы на основе англоязычных "best practices".
Вам объяснят, что const enum решает проблему веса, но ломает некоторые инструменты сборки (Babel), поэтому as const — самый безопасный выбор.
🇬🇧 На английском языке (Первоисточники и авторитетные мнения)
"The Flag That Kills TypeScript Enums" (Matt Pocock) Мэтт Покок — один из самых известных популяризаторов TS. Он выпустил видео и статьи именно про флаг erasableSyntaxOnly (который изначально назывался --isolatedDeclarations в контексте ограничений, а теперь выделился в отдельную опцию для Node.js).
О чем: Он прямо говорит, что этот флаг — гвоздь в крышку гроба enum, так как Node.js теперь умеет запускать TS нативно (удаляя типы), но не умеет компилировать enum.
TypeScript 5.8 Beta Announcement (Microsoft Blog) В официальном блоге разработчиков TypeScript при анонсе версии 5.8 (где добавили erasableSyntaxOnly) прямым текстом написано, зачем это нужно.
Цитата: "Most TypeScript syntax is eraseable... exceptions are namespaces and enums."
Они объясняют, что цель — совместимость с инструментом Node.js --experimental-strip-types.
Google TypeScript Style Guide У Google есть свой стайлгайд. Раньше они были строго против enum, сейчас они допускают их использование, но с оговорками. Однако многие другие крупные стайлгайды (например, Airbnb или стайлгайды популярных библиотек) рекомендуют избегать их.
"Tearing Down TypeScript Enums" (Line Engineering Blog) Хорошая техническая статья, где разбирается 3 типа энамов (Numeric, String, Const) и почему каждый из них имеет свои подводные камни по сравнению с простым Union Type (type A = 'a' | 'b').



*/
